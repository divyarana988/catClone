{
  "name": "catclone",
  "version": "1.0.0",
  "description": "It is clone of cat command available in bash but not in window cmd.\r It is used to display or make a copy content of one or more files in terminal.",
  "main": "catCloneCommand.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "fs": "0.0.1-security",
    "path": "^0.12.7"
  }
}
